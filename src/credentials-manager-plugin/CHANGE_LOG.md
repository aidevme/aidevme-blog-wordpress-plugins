# Credentials Manager Plugin — Change Log

The full version-by-version history of every change made to this plugin, moved out of `SPECIFICATION.md` §10 (which now just points here) to keep that document focused on describing the plugin as it currently is, rather than how it got there.

Every other section of `SPECIFICATION.md` that says "see §10 vNN" is referring to the numbered entry below with that same `vNN` — the numbering here is unchanged from when it lived in the spec, it's simply been relocated.

This is the **ninth** iteration of the underlying spec document. For anyone who saw an earlier version:

- **v1 (custom table + hand-rolled CRUD, no admin UI):** superseded when the URL requirement `/wp-json/wp/v2/credentials` came in — that URL shape is only automatic for a registered post type.
- **v2 (Custom Post Type + native `wp/v2` REST + a classic meta box UI):** delivered the REST URL, but ran into a "critical error" on Publish/Update, most likely from Gutenberg's REST-based save path colliding with the classic meta box's plain-form-POST design. Superseded by dropping the REST/CPT approach entirely: **not** a custom post type, with an admin UX modeled on Contact Form 7 instead.
- **v3 (custom tables, second entity called "Credential Form"):** the working design, just under different names — a `credential_forms` table with a `form_key` column, a `Credpl_Admin_Forms`/`Credpl_Forms_List_Table` admin UI, and a `[credential-form id="…"]` shortcode.
- **v4 renames "Credential Form" to "Credential Block" throughout**, including the parts that matter for already-running sites:
  - **Database**: `Credpl_Installer::maybe_rename_forms_table_to_blocks()` runs before `dbDelta()` in `install()`. If `{$wpdb->prefix}credential_forms` exists and `{$wpdb->prefix}credential_blocks` doesn't yet, it runs `RENAME TABLE … TO …` (preserving every row) and `ALTER TABLE … CHANGE form_key block_key …` to rename that column too. `CREDPL_DB_VERSION` was bumped (`1.0` → `1.1`) specifically to make `maybe_upgrade()` trigger this on sites that already activated an earlier build. If both tables somehow already exist, the migration is skipped rather than risking clobbering data — that scenario isn't expected in normal use.
  - **Shortcode**: `[credential-form id="…"]` → `[credential-block id="…"]`. This is a breaking change for any page/post that already has the old shortcode pasted into its content — WordPress does not alias old shortcode tags to new ones automatically, and this spec doesn't register one. Existing content needs to be found and updated by hand (or via a one-off search-and-replace across `wp_posts.post_content`) after upgrading.
  - **Classes/files**: `Credpl_Admin_Forms` → `Credpl_Admin_Blocks` (`class-credpl-admin-forms.php` → `class-credpl-admin-blocks.php`), `Credpl_Forms_List_Table` → `Credpl_Blocks_List_Table` (`class-credpl-forms-list-table.php` → `class-credpl-blocks-list-table.php`).
  - **`Credpl_Data` methods**: every `*_credential_form*()`/`forms_table()`/`form_key_exists()` method renamed to its `*_credential_block*()`/`blocks_table()`/`block_key_exists()` equivalent (see SPECIFICATION.md §6.6).
  - **Admin menu**: `PAGE_FORMS`/`PAGE_FORM_NEW` → `PAGE_BLOCKS`/`PAGE_BLOCK_NEW`; labels "All Credential Forms"/"Add Credential Form" → "All Credential Blocks"/"Add Credential Block" (§6.1) — the requirement that the top-level menu item still lands on this primary list carries forward unchanged, just pointed at the renamed page.
  - **Front end**: the wrapper CSS class `.credpl-credential-form` → `.credpl-credential-block`, the `data-credpl-form` attribute → `data-credpl-block`, and the stylesheet file `assets/css/credpl-credential-form.css` → `assets/css/credpl-credential-block.css`.
  - **Not renamed**: the Credential (record) edit screen's own `<div id="credpl-credential-form-root">` mount point and its `wp_localize_script` handle `credpl-credential-form`/`credplCredentialForm` — despite containing the word "form," these refer to the HTML `<form>` used to edit a single Credential (§6.2), an unrelated concept to the Credential Block entity being renamed here, and were deliberately left alone.
  - `badge_media`, `credential_link`, `badge_link`, `issuer`, and `title` (on both tables) all carry over conceptually unchanged through every iteration.
  - `tags` (added in v2 to mirror the WP REST Posts schema, then resolved to a real `post_tag` taxonomy relationship) has **no equivalent** from v3 onward — it was entirely a byproduct of aligning with the core Posts REST schema, which no longer applies now that this isn't a post type. Flag if tagging/categorizing credentials is still wanted; it would need its own mechanism.
- **v5 converts both admin React apps from JavaScript to TypeScript** (§6.2.1): `src/index.js` → `src/index.tsx`, `src/credential-block-picker.js` → `src/credential-block-picker.tsx`. Purely a build-time/tooling change — no database, REST, shortcode, or PHP-facing behavior changed; `webpack.config.js`'s entry map, `package.json` (new `typescript`/`@types/react`/`@types/react-dom` devDependencies, new `check-types` script), and a new `tsconfig.json` were the only other files touched.
- **v6 renames the Credential form's source/build files** so the webpack entry key matches its source basename, the same convention the Credential Block picker already followed: `src/index.tsx` → `src/credential.tsx`, and correspondingly `build/index.js`/`build/index.asset.php` → `build/credential.js`/`build/credential.asset.php`. This **is** a real (if small) deployment consideration: `Credpl_Admin_Credentials::enqueue_assets()` now reads `build/credential.asset.php` and enqueues `build/credential.js` — a site running an older build of this plugin (still shipping `build/index.js`) must be updated to the new `dist/credentials-manager-plugin.zip` for the Add/Edit Credential screen to keep loading its script. Nothing about the `wp_localize_script` handle name (`credpl-credential-form`/`credplCredentialForm`) or the mount point (`#credpl-credential-form-root`) changed — those were already correctly named for the *form*, not the file, and were left alone per the v4 note above.
- **v7 absorbs the Credential Block screen's PHP-rendered Title field/`<form>`/Save button into React**, mirroring §6.2's Credential form, and merges the standalone drag-and-drop picker into it as one component tree: `src/credential-block-picker.tsx` (picker-only) is deleted; its component code moves into a new `src/credentials-block.tsx`, which also gains the Title `Field`/`Input` and the `<form>`/hidden-inputs/Save `Button` markup that used to be PHP in `Credpl_Admin_Blocks::render_edit_page()`. Renamed along the way, since these names described "just the picker" and now describe the whole form:
  - **Build output**: `build/credential-block-picker.js`/`build/credential-block-picker.asset.php` → `build/credentials-block.js`/`build/credentials-block.asset.php` — same deployment consideration as v6: an older build's zip won't have these new filenames.
  - **Script handle / localized global**: `credpl-credential-block-picker`/`credplCredentialBlockPicker` → `credpl-credential-block-form`/`credplCredentialBlockForm`, now carrying `id`/`nonce`/`actionUrl`/`title`/`addCredentialUrl` (previously rendered by PHP directly into the `<form>`) alongside the existing `allCredentials`/`selectedIds`.
  - **Mount point**: `#credpl-credential-block-picker-root` → `#credpl-credential-block-form-root`, and it's no longer nested inside a PHP `<form>` — the React component renders its own `<form method="post" action={actionUrl}>` now, same pattern as §6.2.2's Credential form.
  - **Not changed**: `Credpl_Admin_Blocks::save()` — the submitted field names (`title`, `credential_ids[]`, `id`, `_wpnonce`, `action`) are identical to what the old PHP-rendered form sent, so the save handler needed no changes, same as every prior React conversion in this plugin.
- **v8 removes the Badge Link field entirely** — from the Credential form and from `credentials`. Unlike every earlier change, this is the first one to actually **drop** a column rather than add or rename one, which `dbDelta()` cannot do on its own (§4.5): `Credpl_Installer::maybe_drop_badge_link_column()` runs an explicit `ALTER TABLE … DROP COLUMN badge_link` (mirroring the table-rename migration's pattern), gated behind `CREDPL_DB_VERSION` bumping `1.2` → `1.3` so `maybe_upgrade()` runs it once on already-active sites; on a fresh install it's a no-op since `credentials` doesn't exist yet when it runs. Also updated: `credpl-credential-form`/`credplCredentialForm` no longer carries a `badgeLink` field, `Credpl_Admin_Credentials::save()` no longer reads/sanitizes `$_POST['badge_link']`, and `Credpl_Shortcode::render_item()`'s front-end link now uses `credential_link` alone — it previously fell back to `badge_link` when `credential_link` was empty, so a credential that only ever had a Badge Link set (and no Credential Link) will render unlinked after upgrading. `badge_media`/the badge *image* (uploaded via the media library) is a separate field and is unaffected — only the manually-entered badge *URL* is gone.
- **v9 redesigns the shortcode's front-end output** to a vertical stacked list with a small (40px) badge icon, and adds two pieces of information that were collected on the Credential form (§6.2) but never actually shown on the front end: the `title` shortcode attribute — previously parsed but completely unused (per the v8-and-earlier note above, informational only, "mirrors Contact Form 7's `title` attribute") — now renders as a heading when non-empty, and each credential's `earned_on`/`expires_on` now render as an "Expires on … · Earned on …" line (`Credpl_Shortcode::format_earned_expires()`, formatted via `date_i18n()` against the site's `date_format` option). Purely a template/CSS change — no schema, save-handler, or localized-data change, so unlike v6–v8 there's no `CREDPL_DB_VERSION` bump and no rebuilt `.js`/`.asset.php` needed, just the plugin version bump and a fresh `dist/credentials-manager-plugin.zip`. Sites that already have the shortcode pasted into content don't need to touch it — the existing `title="…"` attribute `build_tag()` always included just starts rendering as a heading once the plugin updates.
- **v10 removes the shortcode's `title` attribute** — the front-end heading now always comes straight from the Credential Block's own stored `title` (§4.2), never from a per-shortcode-instance override. v9 had made the `title` attribute affect rendering but left it independently editable per shortcode instance ("Not re-validated against the stored block title"); in practice that let a page's heading silently drift from the block's admin-facing name if someone hand-edited the pasted tag. `Credpl_Shortcode::render()` no longer declares `title` in its `shortcode_atts()` pairs and passes `$block['title']` straight to `render_html()` instead of `$atts['title']`; `Credpl_Shortcode::build_tag()` now generates `[credential-block id="…"]` with no `title="…"` at all. Purely a template change — no schema, save-handler, or localized-data change, so no `CREDPL_DB_VERSION` bump, just the plugin version bump and a fresh `dist/credentials-manager-plugin.zip`. Sites with the shortcode already pasted into content don't need to touch it — a `title="…"` attribute left over from before the upgrade is simply ignored, and the heading switches to tracking the block's own Title field once the plugin updates; if that stored title has since diverged from what a hand-edited `title="…"` used to show, the front end will start displaying the block's title instead.
- **v11 adds a Credentials Type field to `credentials`** — a `credentials_type varchar(50) NOT NULL DEFAULT ''` column (§4.1) holding one of a fixed set of values (`Applied Skills`, `Certifications`, `Awards` — `Credpl_Admin_Credentials::CREDENTIALS_TYPES`), chosen via a Fluent UI `Dropdown` on the Add/Edit Credential form (§6.2) placed immediately after Title. Same category of change as v2's `credential_id`/`certification_number`/etc. additions above: `dbDelta()` adds the new column automatically, so this only needed `CREDPL_DB_VERSION` bumping `1.3` → `1.4` (no explicit migration step) plus the corresponding addition to `Credpl_Installer::missing_credential_columns()`'s expected-columns list. Unlike a free-text field, the value is constrained server-side: `Credpl_Admin_Credentials::sanitize_credentials_type()` checks the submitted value against `CREDENTIALS_TYPES` and stores `''` for anything outside it (a raw POST could submit any string regardless of what the Dropdown client-side offered) — the same allow-list is passed to the React form as `credentialsTypeOptions` (via `wp_localize_script()`) so the Dropdown's own options and the server's validation can't drift apart. Fluent UI 9's `Dropdown` renders a custom listbox rather than a native `<select>`, so — like `DatePicker` before it — it's paired with a hidden `<input type="hidden" name="credentials_type" value={ credentialsType } />` to actually carry the value through the plain form POST. Also shown as a new **Type** column on the Credentials list table (§6.2, immediately after Title — see §6.2 for the table's current column order, which has since been extended and reordered further), rendering `—` for credentials saved before this field existed (or left unset). Not currently surfaced on the front end (the `[credential-block]` shortcode's per-credential output, §7) — extending that to display it would be a separate follow-up if wanted.
- **v12 stops rendering `issuer` in the `[credential-block]` shortcode's front-end output** — `Credpl_Shortcode::render_item()` no longer outputs the `<p class="credpl-credential-issuer">` line, and the now-unused `.credpl-credential-issuer` CSS rule is removed from `credpl-credential-block.css`. `issuer` is still collected on the Credentials form and still shown in its own Credentials list table column (§6.2) — this only affects the public-facing shortcode output. Purely a template/CSS change — no schema, save-handler, or localized-data change, so no `CREDPL_DB_VERSION` bump, just the plugin version bump and a fresh `dist/credentials-manager-plugin.zip`.
- **v13 shrinks the shortcode's per-credential title text** — `.credpl-credential-title`'s `font-size` in `credpl-credential-block.css` changed from `1em` (i.e. the same size as surrounding body text) to `0.9em`, so it reads a bit smaller than a theme's default paragraph text rather than matching it exactly. Purely a CSS change — no schema, save-handler, or localized-data change, so no `CREDPL_DB_VERSION` bump, just the plugin version bump and a fresh `dist/credentials-manager-plugin.zip`. As with every other rule in this stylesheet, the selector stays deliberately low-specificity, so a theme's own more specific heading styles can still override this value.
- **v14 adds a Description field to `credential_blocks`** — a `description text` column (§4.2), mirroring `credentials.description` (§4.1) added back in v2. Same category of change as v2/v11: `dbDelta()` adds the new column automatically, so this only needed `CREDPL_DB_VERSION` bumping `1.4` → `1.5` (no explicit migration step, and — unlike `credentials` — `credential_blocks` has no `missing_credential_columns()`-style ALTER-privilege detection, so a failed automatic `ALTER TABLE` on this table would surface as a raw DB error on save rather than the friendly admin notice). Shown as a Fluent UI `Field`/`Textarea` on the Add/Edit Credential Block form (§6.3.1), placed right after Title and before the Credentials picker; `Textarea` forwards a plain `name="description"` straight through to its native `<textarea>` (same as `Input`), so — unlike `Dropdown`/`DatePicker` elsewhere in this plugin — it needs no paired hidden input to submit through the plain form POST. Sanitized server-side with `sanitize_textarea_field()` (§8), same as `credentials.description`. Also shown as a new **Description** column on the Credential Blocks list table (§6.3, after Shortcode), rendering `—` when not set. Admin-only: not currently rendered in the `[credential-block]` shortcode's front-end output (§7) — extending that to show it would be a separate follow-up if wanted.
- **v15 adds a Description column to the Credentials list table** (§6.2), at the end, after Badge — rendering `—` when not set. `credentials.description` has existed since v2 and was already collected on the Add/Edit Credential form (a `Field`/`Textarea`, §6.2), just never shown in the list; this only touches `Credpl_Credentials_List_Table`. Purely a PHP/list-table change — no schema, save-handler, or localized-data change, so no `CREDPL_DB_VERSION` bump and no rebuilt `.js`/`.asset.php` needed, just the plugin version bump and a fresh `dist/credentials-manager-plugin.zip`.
- **v16 makes the Credentials list table's Title, Type, Issuer, Earned On, and Expires On columns sortable** — `Credpl_Credentials_List_Table::get_sortable_columns()` (standard `WP_List_Table` mechanism) marks those five column headers clickable; `prepare_items()` reads `$_GET['orderby']`/`$_GET['order']` (each `sanitize_key()`'d) and passes them to `Credpl_Data::get_credentials()`, whose existing `$orderby`/`$order` allowlist (in place since the very first version of this table, previously just `id`/`title`/`issuer`/`created_at`) is extended to also accept `credentials_type`, `earned_on`, `expires_on` — anything outside that allowlist still falls back to `title`/`ASC`, same as before (§8). Badge and Description are intentionally left out of `get_sortable_columns()` — a media attachment ID and free-text notes aren't meaningful to sort by. Purely a PHP/list-table change — no schema, save-handler, or localized-data change, so no `CREDPL_DB_VERSION` bump and no rebuilt `.js`/`.asset.php` needed, just the plugin version bump and a fresh `dist/credentials-manager-plugin.zip`.
- **v17 adds a Status field to `credentials`** — a `status varchar(20) NOT NULL DEFAULT ''` column (§4.1) holding one of a fixed set of values (`Active`, `Expired` — `Credpl_Admin_Credentials::STATUSES`), chosen via a Fluent UI `Dropdown` on the Add/Edit Credential form (§6.2), placed right after Credentials Type. Exactly the same pattern as v11's Credentials Type field: `dbDelta()` adds the column automatically, so this only needed `CREDPL_DB_VERSION` bumping `1.5` → `1.6` plus the corresponding addition to `Credpl_Installer::missing_credential_columns()`'s expected-columns list; the value is constrained server-side (`Credpl_Admin_Credentials::sanitize_status()`, values outside `STATUSES` stored as `''`) with the same allow-list passed to the React form as `statusOptions` so the Dropdown's own options and the server's validation can't drift apart; and, like `credentials_type`/`Dropdown` elsewhere in this plugin, it's paired with a hidden `<input type="hidden" name="status" value={ status } />` since Fluent UI's `Dropdown` isn't a native `<select>`. Deliberately a manual field, not derived from `expires_on` — a credential's `status` is whatever the admin sets it to on the form, independent of whether `expires_on` has actually passed. Not currently surfaced in the `[credential-block]` shortcode's front-end output (§7) — extending that to show it would be a separate follow-up if wanted.
- **v18 adds a Status column to the Credentials list table** (§6.2), after Expires On, before Badge — rendering `—` when not set. Not made sortable (unlike Title/Type/Issuer/Earned On/Expires On, §6.2) — not requested, and `credentials_type` already establishes the pattern for a sortable dropdown-backed column if wanted later. Purely a PHP/list-table change — no schema, save-handler, or localized-data change, so no `CREDPL_DB_VERSION` bump and no rebuilt `.js`/`.asset.php` needed, just the plugin version bump and a fresh `dist/credentials-manager-plugin.zip`.
- **v19 adds a third table, `{$wpdb->prefix}microsoft_certifications`, with its own full admin screen** (§4.3, §6.4) — a standalone reference table of Microsoft Learn certification catalog entries (`uid`, `title`, `subtitle`, `url`, `icon_url`, `last_modified`, `type`, `certification_type`, `exams`, `levels`, `roles`, `study_guide`), with no relationship to `credentials`/`credential_blocks` (§3). `Credpl_Installer::install()` creates it via `dbDelta()` alongside the other two tables, gated behind `CREDPL_DB_VERSION` bumping `1.6` → `1.7`; unlike `credentials`, it gets no `missing_credential_columns()`-style ALTER-privilege detection, matching `credential_blocks`'s (not `credentials`') level of safety-checking. New classes mirror the existing per-table architecture: `Credpl_Ms_Certifications_List_Table` (list, sortable Title/Certification Type/Type/Last Modified) and `Credpl_Admin_Ms_Certifications` (page callbacks, save/delete — `credpl_save_ms_certification`/`credpl_delete_ms_certification`), plus a third `Credpl_Data` section (`get_ms_certifications()`/`get_ms_certification()`/`insert_ms_certification()`/`update_ms_certification()`/`delete_ms_certification()`/`ms_certification_uid_exists()`). A third React entry, `src/ms-certification.tsx` (webpack multi-entry map extended accordingly, `build/ms-certification.js`/`.asset.php`), needed no new npm dependencies — every field is a plain Fluent UI `Input`/`Textarea` (no `Dropdown`, no `DatePicker`, no hidden-input pairing anywhere on this form, unlike the other two), including a native `<input type="datetime-local">` for `last_modified` and newline-per-item Textareas for `exams`/`levels`/`roles` (JSON arrays, parsed server-side — §6.4 for the full field-by-field rundown). Two new submenu items (**All Microsoft Certifications**, **Add Microsoft Certification**) bring the top-level menu from five to seven items (§6.1). Not exposed via the `[credential-block]` shortcode or anywhere else on the front end (§3, §7) — admin-only, by design.
- **v20 adds a "Sync Certifications" action to the Microsoft Certifications list screen** (§6.4) — a nonce-protected `page-title-action` link (`admin-post.php?action=credpl_sync_ms_certifications` → `Credpl_Admin_Ms_Certifications::sync()`, gated behind a JS `confirm()`, same "nonce-protected `<a>` link" pattern already used for row-level Delete) that fetches `CATALOG_API_URL` (`https://learn.microsoft.com/api/catalog/?type=certifications,exams&locale=en-us`, a fixed constant — no SSRF surface) via `wp_remote_get()` and upserts one row per returned certification, matched by `uid` (`Credpl_Data::get_ms_certification_by_uid()`, new). Every field from the API response is sanitized through the same field-by-field rules `save()` already applies to `$_POST` (`map_catalog_certification()` — reusing `sanitize_string_list()`/`sanitize_json_field()`/etc., §8), with one addition: `sanitize_iso8601_to_mysql()` converts the API's timezone-aware `last_modified` (e.g. `2025-02-05T01:17:00+00:00`) to UTC via a Unix timestamp, unlike the form's own `sanitize_datetime()`, which has no offset to convert in the first place. Redirects back with `synced=1` plus `sync_created`/`sync_updated`/`sync_skipped` counts — prefixed `sync_` specifically so they can't collide with the plain `updated`/`deleted` query-arg flags `save()`/`delete()` already use on redirects to this same screen (an early version of this without the prefix meant a sync that updated exactly one row also triggered the unrelated single-row "Microsoft Certification saved." notice). A single synchronous request/response cycle — no AJAX, no cron/background job — with a best-effort `set_time_limit( 120 )` (suppressed, gated behind `function_exists()`) given the catalog's size; acceptable since this is a manually-triggered, infrequent, admin-only action, not a page load. Purely additive — no schema change, no `CREDPL_DB_VERSION` bump, no rebuilt `.js`/`.asset.php` (React form unaffected), just the plugin version bump and a fresh `dist/credentials-manager-plugin.zip`.
- **v21 makes sync() skips diagnosable** — the very first real-world sync run (on a site with a prior history of restricted database privileges, v6/v14) returned "0 created, 0 updated, 151 skipped" with no way to tell why, since v20's loop only counted skips without recording a reason. `sync()` now tracks the reason for the *first* skip it hits (one of: an entry that wasn't a valid object, an entry with no `uid`, or the actual database error message from a failed `insert_ms_certification()`/`update_ms_certification()` call) and stashes it via the same transient `store_error_notice()` already uses for save() errors; `maybe_render_notice()` appends it to the sync summary as **"First reason: …"** whenever `sync_skipped > 0`. A uniform 100%-skipped result like the one above most likely means `{$wpdb->prefix}microsoft_certifications` itself doesn't exist yet on that site — `dbDelta()` creating it can fail exactly as silently as the `ALTER TABLE`s described in §5/v6, and unlike `credentials`, this table has no `missing_credential_columns()`-equivalent check to catch that and warn about it (a known gap noted when the table was added, v19) — reactivating the plugin re-runs `Credpl_Installer::install()` unconditionally and would create the table if that's the cause. Purely a diagnostics change — no schema, save-handler behavior, or localized-data change, so no `CREDPL_DB_VERSION` bump and no rebuilt `.js`/`.asset.php` needed, just the plugin version bump and a fresh `dist/credentials-manager-plugin.zip`.
- **v22 adds a fourth table, `{$wpdb->prefix}microsoft_exams`, with its own full admin screen** (§4.4, §6.5) — a sibling reference table to `microsoft_certifications` (v19) for individual Microsoft Learn exam catalog entries (`uid`, `title`, `subtitle`, `display_name`, `url`, `icon_url`, `locales`, `last_modified`, `type`, `courses`, `levels`, `roles`, `products`, `providers`, `study_guide`). Same "standalone, no enforced relationship" status as Microsoft Certifications (§3) — a certification's `exams` array (§4.3) names exam `uid`s that correspond to rows here, but nothing validates or links them. `Credpl_Installer::install()` creates it via `dbDelta()` alongside the other three tables, gated behind `CREDPL_DB_VERSION` bumping `1.7` → `1.8`; same level of safety-checking as `microsoft_certifications`/`credential_blocks` (no `missing_credential_columns()`-equivalent). New classes mirror the existing per-table architecture exactly: `Credpl_Ms_Exams_List_Table` (list, sortable Title/Display Name/Type/Last Modified) and `Credpl_Admin_Ms_Exams` (page callbacks, save/delete — `credpl_save_ms_exam`/`credpl_delete_ms_exam` — no Sync action yet at this point, see v23), plus a fourth `Credpl_Data` section (`get_ms_exams()`/`get_ms_exam()`/`get_ms_exam_by_uid()`/`insert_ms_exam()`/`update_ms_exam()`/`delete_ms_exam()`/`ms_exam_uid_exists()`). A fourth React entry, `src/ms-exam.tsx` (webpack multi-entry map extended accordingly, `build/ms-exam.js`/`.asset.php`, styles in `src/styles/msExam.styles.ts`), needed no new npm dependencies — same plain `Input`/`Textarea`-only form as Microsoft Certifications, including a native `<input type="datetime-local">` for `last_modified` and newline-per-item Textareas for the six array fields (`locales`/`courses`/`levels`/`roles`/`products`/`providers`). Two new submenu items (**All Microsoft Exams**, **Add Microsoft Exam**) bring the top-level menu from seven to nine items (§6.1). Not exposed via the `[credential-block]` shortcode or anywhere else on the front end (§3, §7) — admin-only, by design.
- **v23 adds a "Sync Exams" action to the Microsoft Exams list screen** (§6.5) — the same architecture as Microsoft Certifications' **Sync Certifications** (§6.4, v20), right down to the `page-title-action` link, nonce protection, and `CATALOG_API_URL` constant (the identical literal URL, `https://learn.microsoft.com/api/catalog/?type=certifications,exams&locale=en-us`, defined separately on `Credpl_Admin_Ms_Exams` rather than shared with `Credpl_Admin_Ms_Certifications` — matching this plugin's established convention of independent per-screen implementations over a shared base class/trait). `Credpl_Admin_Ms_Exams::sync()` reads the same response's `exams` array (where Microsoft Certifications' sync reads `certifications`) via `fetch_catalog_exams()`, maps each entry through `map_catalog_exam()` (the same per-field sanitization rules `save()` applies, §8, with `sanitize_iso8601_to_mysql()` in place of `sanitize_datetime()` for the same timezone-aware-vs-wall-clock reason as Microsoft Certifications), and upserts by `uid` (`Credpl_Data::get_ms_exam_by_uid()`, new). Unlike Microsoft Certifications' sync, this one shipped with the skip-diagnostics behavior (stashing and displaying the first skip's reason, `sync_*`-prefixed redirect query args) from the very first version, rather than needing a v21-style follow-up fix — that lesson was already learned by the time this screen was built. Purely additive — no schema change, no `CREDPL_DB_VERSION` bump, no rebuilt `.js`/`.asset.php` (React form unaffected), just the plugin version bump and a fresh `dist/credentials-manager-plugin.zip`.
- **v24 adds a "Back to All Credentials" button to the Credential Block Add/Edit screen** (§6.3) — shown whenever `$_GET['updated']` is present (i.e. right after a create or update redirects back to this screen), above the read-only shortcode box, linking to `Credpl_Admin_Menu::PAGE_CREDENTIALS`. A pure `Credpl_Admin_Blocks::render_edit_page()` template addition — no schema, save-handler, or localized-data change, so no `CREDPL_DB_VERSION` bump and no rebuilt `.js`/`.asset.php` needed (the button is plain PHP-rendered markup, not part of the React form), just the plugin version bump and a fresh `dist/credentials-manager-plugin.zip`.
- **v25 adds the same "Back to All …" button to the other three Add/Edit screens** — Credentials (§6.2), Microsoft Certifications (§6.4), and Microsoft Exams (§6.5) — each linking to its *own* list (`PAGE_CREDENTIALS`/`PAGE_MS_CERTIFICATIONS`/`PAGE_MS_EXAMS` respectively), unlike the Credential Block screen's button (v24), which deliberately points to a *different* screen (Credentials). Same trigger (`isset( $_GET['updated'] )`, already set by each screen's existing `save()` redirect) and same plain PHP-rendered `<a class="button">` markup — no schema, save-handler, or localized-data change on any of the three, so no `CREDPL_DB_VERSION` bump and no rebuilt `.js`/`.asset.php` needed, just the plugin version bump and a fresh `dist/credentials-manager-plugin.zip`.
- **v26 removes the four "Add …" screens from the visible admin menu** (§6.1) — with every Add/Edit screen now reachable via its list's own **Add New** button, each row's **Edit** link, and (since v24/v25) the edit screen's own **"Back to All …"** button, a fifth, always-visible top-level menu entry per screen was redundant. This version did it via `remove_submenu_page( self::PAGE_BLOCKS, … )`, called right after the matching `add_submenu_page()` for each of the four — **this approach broke direct access to all four pages** (fixed in v28; see that note for why).
- **v27 makes the Credential Blocks list table's Title and Description columns sortable** (§6.3) — the one remaining list table without `get_sortable_columns()` (Credentials, Microsoft Certifications, and Microsoft Exams all already had it, v16/v19/v22). `Credpl_Blocks_List_Table::get_sortable_columns()`/`prepare_items()` follow the exact same pattern as the other three (`$_GET['orderby']`/`$_GET['order']` `sanitize_key()`'d, then re-validated against a fixed allowlist); `Credpl_Data::get_credential_blocks()` gained the same `array $args = array()` shape as `get_credentials()`/`get_ms_certifications()`/`get_ms_exams()` (previously a no-argument, hardcoded `ORDER BY title ASC` method — its one caller, `Credpl_Blocks_List_Table::prepare_items()`, is the only thing that changed to pass arguments). Shortcode is intentionally left out of the sortable set — a generated identifier, not descriptive data, same reasoning as Badge/Icon on the other three tables. Purely a PHP/list-table change — no schema, save-handler, or localized-data change, so no `CREDPL_DB_VERSION` bump and no rebuilt `.js`/`.asset.php` needed, just the plugin version bump and a fresh `dist/credentials-manager-plugin.zip`.
- **v28 fixes the "Add …" menu items broken by v26** — clicking any list's **Add New** button, a row's **Edit** link, or a v24/v25 **"Back to All …"** button started showing WordPress core's own **"Sorry, you are not allowed to access this page."** for all four hidden screens. Root cause: `remove_submenu_page()` strips the target entry from the `$submenu` global, but WordPress core's `get_admin_page_parent()` — called by `user_can_access_admin_page()`, which core runs *before dispatching any* `admin.php?page=…` request, ahead of this plugin's own page callback — resolves a page's parent by searching that exact same `$submenu` array for a matching slug. With the entry removed, no parent is found, `user_can_access_admin_page()` returns `false`, and core `wp_die()`s with that message — for *every* link to the page, not just the sidebar item, since sidebar visibility and page-access resolution turn out to share the same underlying data structure. The fix: `Credpl_Admin_Menu::register()` now registers all nine submenus normally (nothing removed, so `$submenu` — and therefore every direct link — resolves exactly as it did before v26), and a new `hide_add_screens_from_menu()` method, hooked to `admin_head` (global, so it applies site-wide, since the WP admin sidebar itself is global — not gated to this plugin's own screens), prints a small inline `<style>` block targeting each hidden page's link by its exact href (`#adminmenu a[href$="page=credpl-credential-new"]{display:none;}`, one rule per page) to hide the `<li>` visually without touching menu registration at all. Purely a bugfix to v26's approach — same end result (four items hidden from the sidebar), same `CREDPL_DB_VERSION`-unaffected, no-rebuild profile, just the plugin version bump and a fresh `dist/credentials-manager-plugin.zip`.
- **v29 adds Award Category and Technology Area fields to `credentials`** (§4.1) — two `VARCHAR(255) NOT NULL DEFAULT ''` columns, `award_category` and `technology_area`, placed right after `status` in the schema, mirroring its position on the form too (§6.2): both new `Field`/`Input`s sit right after the Status dropdown, grouped with the other classification-ish fields (Credentials Type, Status). Unlike `credentials_type`/`status`, neither is a fixed-option `Dropdown` — no list of allowed values was specified for either, so both are plain free-text `Input`s, sanitized server-side with plain `sanitize_text_field()` (no allow-list). `Credpl_Installer::install()` adds both via `dbDelta()` automatically, gated behind `CREDPL_DB_VERSION` bumping `1.8` → `1.9` (same category of change as v11/v17's `credentials_type`/`status` additions), with the corresponding two entries added to `missing_credential_columns()`'s expected-columns list. Not currently surfaced in the Credentials list table (§6.2) or the `[credential-block]` shortcode's front-end output (§7) — collected on the form only, same "collect first, display later if asked" pattern every other field on this table has followed.
- **v30 makes Award Category and Technology Area conditional on Credentials Type being `Awards`** (§6.2) — a plain `'Awards' === credentialsType && ( … )` guard around both `Field`s in `CredentialForm()` (`src/credential.tsx`); no new prop, no CSS, just JSX conditionally omitted from the render the same way any other React conditional works. No schema or save-handler change — `Credpl_Admin_Credentials::save()`'s existing `isset( $_POST['award_category'] )` fallback (already there since v29) already produces the right behavior once the fields can be absent from the POST body: submitting the form with a non-`Awards` Credentials Type now stores `''` for both, since the corresponding `<input>`s simply aren't in the DOM in that case. This is a genuine behavior change worth flagging: an existing Award-type credential's Award Category/Technology Area are **cleared**, not merely hidden, the next time it's saved with a different Credentials Type selected — see the note under the field table in §6.2 for the full reasoning, including why the two fields *don't* lose their values on an in-session toggle back to `Awards` before submitting (client-side React state for both is untouched by visibility). Purely a template/JSX change — no `CREDPL_DB_VERSION` bump, just the plugin version bump, a rebuilt `build/credential.js`/`.asset.php`, and a fresh `dist/credentials-manager-plugin.zip`.
- **v31 renders Award Category and Technology Area on the `[credential-block]` shortcode's front-end output, for `Awards`-type credentials only** (§7) — `Credpl_Shortcode::render_item()` appends up to two further `<p class="credpl-credential-meta">` lines (reusing the existing muted-meta styling, no new CSS) after the existing Expires/Earned line: **"Award Category: …"** and **"Technology Area: …"**, each only when that credential's `credentials_type` is exactly `Awards` (the same literal `Credpl_Admin_Credentials::CREDENTIALS_TYPES` value the form's own v30 conditional checks) *and* the corresponding field is actually non-empty — a credential of any other type never shows these lines, even if `award_category`/`technology_area` happen to still hold a stale value from before it was last saved as a different type (v30's own note explains how that can happen). `issuer` remains deliberately unrendered here (v12) — this only adds the two new fields, nothing about the rest of the template changed. Purely a template/CSS change — no schema, save-handler, or localized-data change, so no `CREDPL_DB_VERSION` bump and no rebuilt `.js`/`.asset.php` needed (this is server-rendered PHP, not part of either React form), just the plugin version bump and a fresh `dist/credentials-manager-plugin.zip`.
- **v32 suppresses the shortcode's "Expires on … · Earned on …" line for `Awards`-type credentials** (§7) — `Credpl_Shortcode::render_item()`'s existing `'' !== $meta` guard around that line (v9) gained a second condition, `'Awards' !== $credential['credentials_type']`, so the line no longer renders at all for an Awards credential even if `earned_on`/`expires_on` happen to be set on it; every other `credentials_type` is unaffected and continues to show the line exactly as before. Rationale: earned/expiry dates aren't a meaningful concept for an award the way they are for a certification, and the screenshot that drove v31's "Award(s)" example (§7) shows the title and badge followed directly by Award Category/Technology Area, with no date line at all. Purely a template change — no schema, save-handler, or localized-data change, so no `CREDPL_DB_VERSION` bump and no rebuilt `.js`/`.asset.php` needed, just the plugin version bump and a fresh `dist/credentials-manager-plugin.zip`.
- **v33 fixes a bullet point appearing in front of each shortcode list item on some sites** (§7) — a live "Applied Skill(s)" block showed a `•`/`.`-style marker sitting on its own line above each entry's badge/title row, even though `.credpl-credential-list` already sets `list-style: none`. Root cause: that rule is deliberately low-specificity (§7 item 4), so a theme's own content-area list styling (e.g. `.entry-content ul`) can override it depending on the theme's own selector specificity/source order — and because `.credpl-credential-item` is `display: flex` (needed for the badge-beside-title layout), a marker that does get through renders as its own line above the flex row rather than inline beside the text the way a normal `<li>` marker would, which is what made the bug visually obvious. Fix: `.credpl-credential-list` and `.credpl-credential-item` both now carry `list-style: none !important`, plus a `.credpl-credential-item::marker { content: none; }` fallback for browsers/themes that reintroduce a marker via `::marker` specifically rather than `list-style`. This is now the one `!important` in `credpl-credential-block.css` — an intentional, narrowly-scoped exception to the stylesheet's usual "stay low-specificity so themes can override" convention, since a stray bullet is a plugin-owned layout bug, not something a theme should reasonably be able to reintroduce. Purely a CSS change — no schema, save-handler, or localized-data change, so no `CREDPL_DB_VERSION` bump and no rebuilt `.js`/`.asset.php` needed, just the plugin version bump and a fresh `dist/credentials-manager-plugin.zip`.
- **v34 converts the Credentials list screen from a `WP_List_Table` to a React app built on Fluent UI 9's `DataGrid`** (§6.2.3) — a pilot for moving this plugin's admin screens onto the same Fluent UI/React stack the four Add/Edit forms already use (§6.2/§6.3/§6.4/§6.5), starting with the busiest list screen. `Credpl_Credentials_List_Table` (`includes/class-credpl-credentials-list-table.php`) is deleted outright — nothing else referenced it once `Credpl_Admin_Credentials::render_list_page()` stopped calling it — and a new `enqueue_list_assets()` method (split out of `enqueue_assets()`, which now dispatches to either it or the existing form-enqueuing logic based on `$_GET['page']`) runs the exact same `Credpl_Data::get_credentials()` query the old list table ran, then localizes the resulting rows (plus pre-built Edit/Delete URLs and pre-formatted dates, same as the old list table computed) to `window.credplCredentialsList` for a new `src/credentials-list.tsx` entry to render. This is a **server-rendered-once** data flow, not a REST-backed one — PHP still queries and sorts exactly once per page load, same as before; sorting a column still does a full page reload (`DataGrid`'s `onSortChange` navigates to `?orderby=…&order=…` instead of re-sorting client-side), so no new REST route or nonce surface was introduced. Columns and their sortability are unchanged from the old list table (Title/Type/Issuer/Earned On/Expires On sortable; Status/Badge/Description not) except for one addition: a dedicated **Actions** column (Edit/Delete `Button`s) replaces `WP_List_Table`'s hover-revealed row-actions text, since `DataGrid` has no equivalent built-in convention and an always-visible column is more accessible than a hover-only one. The other three list tables (`Credpl_Blocks_List_Table`, `Credpl_Ms_Certifications_List_Table`, `Credpl_Ms_Exams_List_Table`, §6.3/§6.4/§6.5) are untouched by this version — converting them the same way would be a separate follow-up if wanted, now that this pattern exists to copy. No schema change, so no `CREDPL_DB_VERSION` bump; this **does** need a rebuilt `build/credentials-list.js`/`.asset.php` (a new webpack entry, §6.2.1) and a fresh `dist/credentials-manager-plugin.zip`.
- **v35 renders the Credentials list's Status cell as a colored Fluent UI `Badge`** (§6.2.3) instead of plain text — `CredentialStatusCell()` in `src/credentials-list.tsx` renders `<Badge color="success">Active</Badge>` (green) for the literal value `Active`, `<Badge color="danger">Expired</Badge>` (red) for `Expired` — the same two literals `Credpl_Admin_Credentials::STATUSES` (§6.2) already fixes as the Status `Dropdown`'s only options — and an uncolored, default-appearance `Badge` for any other non-empty value (there shouldn't be one, given the Dropdown's fixed options, but a raw-POSTed or pre-existing value outside the allow-list isn't assumed to be one of the two known statuses); a credential with no Status set at all still renders the plain `—` text used everywhere else in this list, not an empty/dashed `Badge`. Purely a template/JSX change to one cell renderer — no schema, save-handler, or localized-data change (the `status` string PHP already sends is unchanged), so no `CREDPL_DB_VERSION` bump; this **does** need a rebuilt `build/credentials-list.js`/`.asset.php` (same entry as v34, not a new one) and a fresh `dist/credentials-manager-plugin.zip`.
- **v36 makes the Credentials list's Title a Fluent UI `Link` and truncates/tooltips the Description cell** (§6.2.3) — two independent, small `src/credentials-list.tsx` cell-renderer changes: (1) `CredentialTitleCell()` wraps the title in a Fluent UI `Link href={ row.editUrl }` instead of rendering it as plain text, so clicking the title itself navigates to the credential's Edit screen — the same destination (and the same nonce-free `admin.php?page=…&id=…` URL) the Actions column's Edit `Button` already links to, just a second way to get there, matching the old `WP_List_Table`'s convention of making the title itself a link (§6.2's pre-v34 `column_title()`, which this brings back after v34's conversion dropped it in favor of the dedicated Actions column alone); (2) `CredentialDescriptionCell()` renders the description text inside a fixed-max-width `<span>` (`descriptionCell` in `credentialsList.styles.ts` — `overflow: hidden`, `whiteSpace: nowrap`, `textOverflow: ellipsis`) wrapped in a Fluent UI `Tooltip` (`relationship="label"`) carrying the untruncated text, so a long description that would otherwise blow out the row is visually clipped with `…` but still fully readable on hover/focus — a credential with no description at all still renders the plain `—` used everywhere else in this list, with no `Tooltip` wrapping it (nothing to show on hover). Purely template/JSX/CSS changes to two cell renderers — no schema, save-handler, or localized-data change (PHP already sends the full `description` string and `editUrl`; nothing new was added to `window.credplCredentialsList`), so no `CREDPL_DB_VERSION` bump; this **does** need a rebuilt `build/credentials-list.js`/`.asset.php` (same entry as v34/v35) and a fresh `dist/credentials-manager-plugin.zip`.
- **v37 rebuilds the Credentials list on Fluent UI 9's plain `Table` primitives instead of `DataGrid`** (§6.2.3) — `src/credentials-list.tsx` now renders `Table`/`TableHeader`/`TableHeaderCell`/`TableRow`/`TableBody`/`TableCell` directly, driven by a hand-rolled `ColumnDef[]` array (`{ id, label, sortable, renderCell }`), rather than `DataGrid`/`createTableColumn()`. Every user-visible behavior from v34–v36 is preserved exactly — the same nine columns in the same order, the same Title `Link`/Status `Badge`/Description `Tooltip`+truncation/Actions `Button`s cell renderers (all four carried over unchanged), the same five sortable columns, the same full-page-reload sort-on-header-click behavior (now `buildNextSortUrl()` computing the toggle explicitly, where `DataGrid`'s `onSortChange` used to hand that toggle to `onSortChange` already computed) — this is a swap of the underlying component, not a feature change. Motivation: `DataGrid` is `@fluentui/react-table`'s own opinionated wrapper around these same primitives; this screen's sorting was always server-driven (a full page reload, never a client-side re-sort), so none of what `DataGrid` adds on top — client-side sort-state management, column sizing, selection — was ever actually used, and the plain `Table` primitives are the more direct fit plus a smaller bundle (`build/credentials-list.js` dropped from ~280 KB to ~168 KB minified, since none of `@fluentui/react-table`'s sort/sizing/selection machinery needs bundling any more). No schema, save-handler, or localized-data change — `window.credplCredentialsList`'s shape is untouched — so no `CREDPL_DB_VERSION` bump; this **does** need a rebuilt `build/credentials-list.js`/`.asset.php` (same entry, not a new one) and a fresh `dist/credentials-manager-plugin.zip`.
- **v38 makes the Credentials list multiple-selectable** (§6.2.3) — a leading `TableSelectionCell` column (checkbox cells, `@fluentui/react-table`, re-exported from `@fluentui/react-components`) on both the header row (select/deselect all, showing Fluent's built-in `"mixed"` indeterminate state when some but not all rows are checked) and every body row. Selection is tracked with a plain `useState<Set<number>>` of credential IDs in `CredentialsList()` — not the `useTableFeatures`/`useTableSelection` hook pair `@fluentui/react-table` ships for this, since that pair expects a `DataGrid`-shaped `columns: TableColumnDefinition<TItem>[]` array to drive its bookkeeping, which this screen stopped maintaining when it moved off `DataGrid` onto plain `Table` primitives (v37) — a `Set` is the more direct fit for what's actually needed here. A selected row gets `TableRow appearance="brand"` (matching Fluent's own selection examples) and `aria-selected`. Each row's checkbox is the only thing that toggles it — not the whole row, the more common Fluent selection pattern — because this table already has several independently-clickable cells (the Title `Link`, the Actions column's Edit/Delete `Button`s, v36); a whole-row click handler would have made every click on those also toggle selection as an unwanted side effect. Purely a template/JSX/state addition to `src/credentials-list.tsx` — nothing currently *reads* `selectedIds` beyond driving the header/row checkboxes' own checked state and row highlighting; no bulk action (e.g. a bulk-delete button) was added, since none was asked for — a natural follow-up if wanted. No schema, save-handler, or localized-data change, so no `CREDPL_DB_VERSION` bump; this **does** need a rebuilt `build/credentials-list.js`/`.asset.php` (same entry, not a new one) and a fresh `dist/credentials-manager-plugin.zip`.
- **v39 fixes v38's checkboxes not actually selecting anything, attempt 1: switches the `checkboxIndicator` handler from `onClick` to `onChange`** — `useCheckboxBase_unstable` (`@fluentui/react-checkbox`) wires only `state.input.onChange` to the native `<input>`'s `change` event for detecting a toggle, so v38's `onClick` override was never the hook the component actually reads. Superseded by v40 below, which found this still wasn't reliable enough and moved the handler one level up.
- **v40 fixes the selection checkboxes for real, attempt 2: moves the click handler off the nested `Checkbox` slot entirely, onto `TableSelectionCell`'s own cell** — rather than reaching into `checkboxIndicator` (a slot override on the `Checkbox` Fluent renders *inside* `TableSelectionCell`, whose internal event wiring v39 still couldn't get to behave reliably), `onClick={ toggleAllRows }` / `onClick={ () => toggleRow( row.id ) }` are now passed as a top-level prop directly to `<TableSelectionCell>` itself — which, like any other Fluent slot component, forwards an unmatched top-level prop straight to its own root element (the actual `<td>`/`<th>` cell). A click anywhere inside that cell — including on the checkbox visual itself, since a native click always bubbles from the inner `<input>` up through its ancestors — reaches this handler through plain, guaranteed DOM event bubbling to a native table cell, with no dependency on how `Checkbox` internally wires its own `onChange`/`onClick`. `checkboxIndicator` is still passed, now carrying only the static `'aria-label'` (no handler). Same `Set<number>` state, `"mixed"` header indicator, and "only the checkbox toggles, not the whole row" scoping as v38/v39. Purely a bugfix to the event wiring — no schema, save-handler, or localized-data change, so no `CREDPL_DB_VERSION` bump; this **does** need a rebuilt `build/credentials-list.js`/`.asset.php` (same entry, not a new one) and a fresh `dist/credentials-manager-plugin.zip`.
- **v41 adds a Fluent UI `Toolbar` (New / Edit / a `ToolbarDivider` / Delete) above the Credentials list, wired to `selectedIds`** (§6.2.3) — **New** disabled whenever anything is selected, otherwise navigating to the Add New Credential screen; **Edit** enabled only when exactly one row is selected, navigating to that row's own edit screen; **Delete** enabled whenever one or more rows are selected, confirming then deleting every selected credential at once via a new bulk-delete action. This is the first thing to actually *read* `selectedIds` beyond driving checkbox/row-highlight state (v38's note that nothing did yet is now superseded). New backend surface: `Credpl_Admin_Credentials::BULK_DELETE_ACTION`/`bulk_delete()` — a nonce-protected `admin-post.php` action (one fixed nonce action string, not per-ID, since the selected ID set is only known client-side, §8) that `absint()`-sanitizes `$_GET['ids']`, calls the existing `Credpl_Data::delete_credential()` once per ID, and redirects back with a new `bulk_deleted=<count>` query arg (distinct from the existing single-row `deleted=1` flag, same collision-avoidance convention as the Sync actions' `sync_*` args, v20) — `maybe_render_notice()` shows it as a singular/plural **"N credential(s) deleted."** notice (`_n()`). `Credpl_Admin_Credentials::enqueue_list_assets()` gained two new localized fields (`addNewUrl`, `bulkDeleteUrl`) and two new confirm-dialog strings (`deleteConfirmText` already existed; `bulkDeleteConfirmText` is new) to support this. No schema change, so no `CREDPL_DB_VERSION` bump; this **does** need a rebuilt `build/credentials-list.js`/`.asset.php` (same entry, not a new one) and a fresh `dist/credentials-manager-plugin.zip`.
- **v42 replaces both delete confirmations' `window.confirm()` with a Fluent UI `Dialog`** (§6.2.3) — a new, generic (not Credentials-specific) `ConfirmationDialog` component, `src/components/dialogs/confirmation-dialog.tsx`, wraps Fluent's `Dialog`/`DialogSurface`/`DialogBody`/`DialogTitle`/`DialogContent`/`DialogActions` behind a small `open`/`title`/`message`/`confirmLabel`/`cancelLabel`/`onConfirm`/`onCancel` prop surface, with `modalType="alert"` so it can only be dismissed via an explicit button click, not by clicking the dimmed backdrop. `CredentialsList()` (`src/credentials-list.tsx`) tracks a `pendingDelete: PendingDelete | null` (`{ kind: 'single'; row } | { kind: 'bulk'; ids: number[] }`) — the Actions column's Delete button and the toolbar's Delete button now just set this state instead of navigating (or calling `window.confirm()`) directly; one shared dialog instance renders whenever it's non-null, with its title/message built from whichever kind is pending (a singular message naming the credential's title for `'single'`, a singular-or-plural (`_n()`) count for `'bulk'`), and its `onConfirm` performs the exact navigation the v41/v38 flow used to perform immediately on click (the row's `deleteUrl`, or `buildBulkDeleteUrl( pendingDelete.ids )` — a pure helper, renamed from v41's `handleBulkDelete()` now that it no longer also confirms/navigates itself). `deleteConfirmText`/`bulkDeleteConfirmText` are removed from `window.credplCredentialsList` and from `Credpl_Admin_Credentials::enqueue_list_assets()`'s localized array — the confirmation copy is now generated client-side (`__()`/`_n()`/`sprintf()` from `@wordpress/i18n`), consistent with every other static string already rendered in this screen (column labels, toolbar button labels, etc.), rather than PHP needing to know the dialog's exact phrasing. No schema, save-handler, or backend-behavior change — `bulk_delete()`/the single-row `delete()` handler are untouched, only how confirmation is presented — so no `CREDPL_DB_VERSION` bump; this **does** need a rebuilt `build/credentials-list.js`/`.asset.php` (same entry, not a new one) and a fresh `dist/credentials-manager-plugin.zip`.
- **v43 makes the Credentials list toolbar's three buttons icon-only, each with an explanatory `Tooltip`** (§6.2.3) — `AddRegular`/`EditRegular`/`DeleteRegular` (a new dependency, `@fluentui/react-icons`, §6.2.1) replace New/Edit/Delete's visible text labels via each `ToolbarButton`'s `icon` prop; a Fluent UI `Tooltip` (`relationship="label"`, the same mechanism v36 already used for the Description column) wraps each button and supplies its accessible name plus a full-sentence explanation of what it does and, since that's no longer implied by a text label, when it's enabled. Each button also switched from `disabled` to `disabledFocusable` so its tooltip keeps working (hoverable/focusable) while disabled — a plain `disabled` native `<button>` doesn't reliably receive hover events at all — with no behavior change to what clicking actually does while disabled, since `useARIAButtonProps()` (`@fluentui/react-aria`) already strips the `onClick` handler from the rendered button for both `disabled` and `disabledFocusable` alike. Purely a template/dependency change — no schema, save-handler, or localized-data change, so no `CREDPL_DB_VERSION` bump; this **does** need a rebuilt `build/credentials-list.js`/`.asset.php` (same entry, not a new one) and a fresh `dist/credentials-manager-plugin.zip`.
- **v44 adds a comprehensive `Tooltip` to every Credentials list header cell** (§6.2.3) — the leading select-all checkbox cell and all nine column headers now each wrap their `TableSelectionCell`/`TableHeaderCell` in a `Tooltip` (`relationship="label"`, same mechanism as v36/v43), explaining what the column shows and — for the five sortable columns (`title`/`credentials_type`/`issuer`/`earned_on`/`expires_on`) — that clicking sorts by it and clicking again reverses the order; the four non-sortable columns (`status`/`badge`/`description`/`actions`) say explicitly that they aren't sortable. `ColumnDef` gained a `headerTooltip: string` field to carry this text per column. The table's own sorting mechanism (v37's `TableHeaderCell`/`sortable`/`sortDirection`/full-page-reload approach) is unchanged — this version is documentation-in-the-UI, not a sorting behavior change. Purely a template change — no schema, save-handler, or localized-data change, so no `CREDPL_DB_VERSION` bump; this **does** need a rebuilt `build/credentials-list.js`/`.asset.php` (same entry, not a new one) and a fresh `dist/credentials-manager-plugin.zip`.
- **v45 removes the Credentials list's per-row Actions column entirely** (§6.2.3) — `CredentialsListRowActions()` and its `actions` `ColumnDef` entry are deleted from `src/credentials-list.tsx`, and the now-unused `actionsCell` rule is deleted from `credentialsList.styles.ts`; the table now has eight columns (down from nine) plus the leading selection checkbox. Rationale: the Actions column had become fully redundant — the Title `Link` (v36) already opens a row's edit screen, and the toolbar's Edit/Delete buttons (v41, disabled/enabled by selection count) cover editing and deleting, so a third, always-visible per-row Edit/Delete pair duplicated both. This let two more things simplify: `columns` moved back to module scope (it only needed to live inside `CredentialsList()`, since v42, so the `actions` column could close over `setPendingDelete` — with that column gone, none of the remaining `renderCell`s reference any component state), and the Delete confirmation flow's `PendingDelete` discriminated union (`{ kind: 'single'; row } | { kind: 'bulk'; ids: number[] }`, v42) collapsed to a plain `useState<number[] | null>` (`pendingDeleteIds`), since the `'single'` variant's only caller was the now-deleted row Delete button — the dialog still names a single credential's own title when exactly one ID is pending (looked up from `config.rows` by ID instead of carried directly in the state), so the single-delete UX reads the same as before. Nothing on the backend changed or was removed: `Credpl_Admin_Credentials::DELETE_ACTION`/`delete()` (the single-row admin-post action) and each row's `deleteUrl` are left fully in place, just no longer linked from anywhere in this screen's UI — deleting one credential now goes through selecting its row and the toolbar's Delete button (which was always able to handle exactly one ID, same as several) rather than a dedicated single-delete link. No schema change, so no `CREDPL_DB_VERSION` bump; this **does** need a rebuilt `build/credentials-list.js`/`.asset.php` (same entry, not a new one) and a fresh `dist/credentials-manager-plugin.zip`.
- **v46 renders the Credentials list's header cell labels in semibold** (§6.2.3) — each `TableHeaderCell` gets `style={ { fontWeight: tokens.fontWeightSemibold } }`, an inline style rather than a `className`/`makeStyles` rule, since `TableHeaderCell`'s own root slot already sets a `font-weight: var(--fontWeightRegular)` class of its own and a competing class-based override isn't guaranteed to win the cascade against it (both are Griffel atomic classes of equal specificity, and which one wins depends on stylesheet insertion order, not something this component controls) — an inline style always wins regardless. A dead, never-applied `titleCell` rule already sitting in `credentialsList.styles.ts` (Fluent UI's `Table` primitives conversion, v37, apparently never wired it to anything) is deleted as part of this change, rather than left alongside the new inline style as unused cruft. Purely a visual/CSS change — no schema, save-handler, or localized-data change, so no `CREDPL_DB_VERSION` bump; this **does** need a rebuilt `build/credentials-list.js`/`.asset.php` (same entry, not a new one) and a fresh `dist/credentials-manager-plugin.zip`.
- **v47 adds a pointing arrow to every Fluent UI `Tooltip` on the Credentials list** (§6.2.3) — all six `Tooltip` usages in `src/credentials-list.tsx` (the Description column's truncated-text tooltip, v36; the toolbar's three button tooltips, v43; the select-all checkbox's header tooltip and every column's header tooltip, v44) gained `withArrow`, Fluent UI's own prop for rendering the small triangle connecting the tooltip bubble to whatever it's pointing at (`false` by default). Purely a visual prop addition — no schema, save-handler, or localized-data change, so no `CREDPL_DB_VERSION` bump; this **does** need a rebuilt `build/credentials-list.js`/`.asset.php` (same entry, not a new one) and a fresh `dist/credentials-manager-plugin.zip`.
- **v48 (this version) moves this entire change log out of `SPECIFICATION.md` §10 into this standalone `CHANGE_LOG.md` file** — the spec's §10 heading and its `(§10 vNN)` cross-references sprinkled throughout the rest of the document (§4–§11) are left exactly as they were, now acting as pointers into this file instead of into the spec's own body, so nothing else needed to change. Purely a documentation-organization change — no code, schema, or behavior change of any kind, so no `CREDPL_DB_VERSION` bump and no rebuild needed; the plugin version was still bumped and a fresh `dist/credentials-manager-plugin.zip` produced, for consistency with this plugin's "every documented change gets a version bump" convention even when the change is docs-only.
