import { makeStyles } from '@fluentui/react-components';

export const useMsExamsListStyles = makeStyles( {
	iconImg: {
		width: '32px',
		height: '32px',
		objectFit: 'contain',
		display: 'block',
	},
} );
